**依赖**

安装req.txt中的py软件包
conda activate xx(your pyenv)
pip install req.txt

---
**启动**

cmd/bash启动
streamlit run app.py

---
**说明**

可更改：
目标数量（高价值/总目标）
llm解析和agent博弈模式/简单数理规则模式
可以进行自然语言输入
env修改模型和api key

*示例使用deepseek-chat*

